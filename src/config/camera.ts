export const CAMERA_CONFIG = {
  enabled: true,
  defaultFacingMode: "environment" as "user" | "environment",
  defaultFilter: "normal",
  allowFilterEffects: true,
  allowFaceEffects: true,
  allowMultiplePhotos: true,
  showPlayfulMessages: true,
  showDailyPrompts: true,
  showRandomFilter: true,
  maxPhotosPerEntry: 10,
  maxImageSizeMB: 8,
};
